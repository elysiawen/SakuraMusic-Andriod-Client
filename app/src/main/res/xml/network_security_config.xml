<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- 网关通常跑在局域网里、只有明文 HTTP，因此放开 cleartext。 -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </base-config>
</network-security-config>
