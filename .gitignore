*.iml
.gradle/
# Kotlin 的会话目录，每次构建都会重写。
.kotlin/
local.properties
.idea/
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.log
.env
.env.*

# Release signing material — must never be committed.
# 谁拿到这两个文件，谁就能签出「Android 认可的本应用更新」。
keystore/
*.jks
*.keystore
keystore.properties
keystore.b64.txt
